server:
  port: 8091

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka

spring:
  application:
    name: analytics-streams-service

  kafka:
    bootstrap-servers: localhost:29092

    streams:
      application-id: analytics-streams